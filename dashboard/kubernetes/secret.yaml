apiVersion: v1
kind: Secret
metadata:
  name: factory-secrets
  namespace: factory-digital-twin
type: Opaque
stringData:
  NEO4J_PASSWORD: "factory_twin_2025"
  # In production, use proper secrets management like:
  # kubectl create secret generic factory-secrets \
  #   --from-literal=NEO4J_PASSWORD=<strong-password> \
  #   -n factory-digital-twin
